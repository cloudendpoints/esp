events {
    worker_connections  1024;
}

http {
    server {
        error_log logs/error.log debug;
        listen       8080;
        location / {
            index  index.html index.htm;
        }
    }
}
